# MC-Scheduler Handoff - 2026-04-29

## Session Summary

- Created repository-specific `AGENTS.md` instructions for MC-Scheduler.
- Created a new React and TypeScript app in `mc-scheduler-app`.
- Installed project dependencies.
- Replaced the default Vite starter screen with an initial MC Scheduler app shell.
- Added the first scheduler TypeScript data model.
- Added a legacy backup import compatibility layer for the current JSON-shaped backup format.
- Verified the app builds successfully with `npm.cmd run build`.

## Git Checkpoints

- `70eacf8` - Scaffold desktop app prototype
- `4e8666e` - Add scheduler data model

Both commits were pushed to `origin/main`.

## Current Project Shape

- `scheduler1.66.html` remains untouched as the working prototype/reference.
- `mc-scheduler-app` is the new app folder.
- `mc-scheduler-app/src/domain/schedulerTypes.ts` contains the new app data types.
- `mc-scheduler-app/src/domain/legacyBackup.ts` converts older backup-shaped data into the new model.

## Known Environment Notes

- PowerShell blocks `npm.ps1`, so use `npm.cmd`.
- Build commands needed elevated permission because Vite helper process execution was blocked in the sandbox.
- Git still reports a warning about not being able to read `C:\Users\shaun\.config\git\ignore`.
- Rust/Cargo are not installed or not currently in PATH, so Tauri cannot be added yet.

## Recommended Next Step

Move the scheduling rules from `scheduler1.66.html` into testable TypeScript functions:

- EXD requires ALE.
- Operators can be unavailable by weekday.
- Vacations block assignments.
- Smart assignment respects max shifts.
- Smart assignment can prevent back-to-back shifts.
- Smart assignment can limit weekly shifts.
